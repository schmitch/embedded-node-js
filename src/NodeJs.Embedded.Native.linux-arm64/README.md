# Embedded Node.js (linux-arm64)

Runtime-only package that ships the Node.js executable for linux-arm64.

Usage:
- Install the package alongside `EmbeddedNodeJs` (helper) and `JavaScriptEngineSwitcher.Node`.
- The Node binary will be copied from `runtimes/linux-arm64/native/`.

Version: 0.1.0
