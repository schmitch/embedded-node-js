# Embedded Node.js (osx-x64)

Runtime-only package that ships the Node.js executable for osx-x64.

Usage:
- Install the package alongside `EmbeddedNodeJs` (helper) and `JavaScriptEngineSwitcher.Node`.
- The Node binary will be copied from `runtimes/osx-x64/native/`.

Version: 0.1.0
