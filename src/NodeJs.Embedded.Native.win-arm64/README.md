# Embedded Node.js (win-arm64)

Runtime-only package that ships the Node.js executable for win-arm64.

Usage:
- Install the package alongside `EmbeddedNodeJs` (helper) and `JavaScriptEngineSwitcher.Node`.
- The Node binary will be copied from `runtimes/win-arm64/native/`.

Version: 0.1.0
